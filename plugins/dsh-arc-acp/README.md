# dsh-arc-acp 1.0.0

Configuration-only Cordis Bundle for the optional `acp`
entry of `dsh-arc`. Install it alongside the core into the same DSH Profile.
Removing the bundle disables this integration and preserves persisted session data.
See the repository README for the assembled DSH ARC distribution.
